# COVID-19 ETL Pipeline Configuration
project:
  name: "Python ETL Pipeline Framework"
  version: "1.0.0"
  description: "Modular ETL pipeline framework supporting COVID-19 and music data pipelines"

# Data Sources
data_sources:
  # Johns Hopkins University COVID-19 Data
  jhu:
    base_url: "https://raw.githubusercontent.com/CSSEGISandData/COVID-19/master/csse_covid_19_data/csse_covid_19_time_series"
    files:
      - name: "confirmed_global"
        filename: "time_series_covid19_confirmed_global.csv"
        description: "Global confirmed COVID-19 cases"
      - name: "deaths_global"
        filename: "time_series_covid19_deaths_global.csv"
        description: "Global COVID-19 deaths"
      - name: "recovered_global"
        filename: "time_series_covid19_recovered_global.csv"
        description: "Global COVID-19 recoveries"

  # Our World in Data
  owid:
    base_url: "https://raw.githubusercontent.com/owid/covid-19-data/master/public/data"
    files:
      - name: "owid_covid_data"
        filename: "owid-covid-data.csv"
        description: "Comprehensive COVID-19 dataset with vaccinations and tests"

  # WHO COVID-19 Global Data (currently unavailable)
  # who:
  #   base_url: "https://covid19.who.int"
  #   api_endpoint: "https://covid19.who.int/WHO-COVID-19-global-data.csv"
  #   description: "WHO global COVID-19 data"

  # Billboard Hot 100
  billboard:
    chart_name: "hot-100"
    start_date: "2015-01-01"
    end_date: "2025-01-01"
    sleep_seconds: 1.5
    output_file: "billboard_hot100.csv"

  # Last.fm API
  lastfm:
    base_url: "https://ws.audioscrobbler.com/2.0/"
    api_key_env: "LASTFM_API_KEY"
    api_secret_env: "LASTFM_API_SECRET"
    rate_limit_sleep: 0.2
    billboard_input: "billboard_hot100.csv"
    output_file: "lastfm_track_data.csv"

  # Kaggle — Spotify audio features dataset
  kaggle_spotify:
    dataset: "yamaerenay/spotify-dataset-19212020-600k-tracks"
    source_filename: "tracks.csv"
    output_file: "spotify_audio_features.csv"
    api_key_env: "KAGGLE_KEY"

  # Audio enrichment settings
  audio_enricher:
    spotify_file: "spotify_audio_features.csv"
    match_threshold: 85        # minimum rapidfuzz score (0-100)
    output_file: "music_enriched.csv"
    final_export_file: "music_dataset_final.csv"

# Local Data Paths (relative to project root)
data_paths:
  raw: "data/raw"
  processed: "data/processed"
  output: "data/output"

# Database Configuration
database:
  type: "postgresql"  # or "sqlite" for local development
  # Connection string is read from DATABASE_URL in .env — never hardcode credentials here
  connection_string: "${DATABASE_URL}"
  schema: "public"

# ETL Pipeline Settings
pipeline:
  extract:
    retry_attempts: 3
    retry_delay: 5  # seconds
    timeout: 30  # seconds

  transform:
    # Data quality thresholds
    missing_data_threshold: 0.5  # Remove columns with >50% missing data
    outlier_method: "tukey"  # tukey, zscore, or isolation_forest

    # Feature engineering
    features:
      - "cases_per_million"
      - "deaths_per_million"
      - "mortality_rate"
      - "recovery_rate"
      - "doubling_time"

  load:
    batch_size: 1000
    if_exists: "replace"  # replace, append, or fail

# Logging Configuration
logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  file: "logs/etl_pipeline.log"

# Analysis Settings
analysis:
  target_countries: ["United States", "China", "India", "Brazil", "Russia"]
  date_range:
    start: "2020-01-01"
    end: "2023-12-31"
